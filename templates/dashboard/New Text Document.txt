{% extends "layouts/base.html" %}

{% block extra_css %}
<link
rel="stylesheet"
href="{{ url_for('static', filename='css/devices.css') }}"
>
{% endblock %}

{% block content %}

<div class="devices-container">

    <div class="page-header">

        <h1>
            IoT Device Management
        </h1>

        <p>
            Healthcare Device Monitoring Center
        </p>

    </div>

    <div class="stats-grid">

        <div class="stat-card">

            <h3>Total Devices</h3>

            <span>
                {{ total_devices }}
            </span>

        </div>

        <div class="stat-card">

            <h3>Online</h3>

            <span>
                {{ online_count }}
            </span>

        </div>

        <div class="stat-card">

            <h3>Offline</h3>

            <span>
                {{ offline_count }}
            </span>

        </div>

    </div>

    <div class="dashboard-panel">

        <h2>
            Registered Devices
        </h2>

        <table class="device-table">

            <thead>

                <tr>

                    <th>ID</th>
                    <th>Name</th>
                    <th>Type</th>
                    <th>IP</th>
                    <th>Status</th>
                    <th>Risk</th>
                    <th>Last Seen</th>
                    <th>Action</th>

                </tr>

            </thead>

            <tbody>

                {% for device in devices %}

                <tr>

                    <td>{{ device.id }}</td>

                    <td>{{ device.name }}</td>

                    <td>{{ device.type }}</td>

                    <td>{{ device.ip }}</td>

                    <td>{{ device.status }}</td>

                    <td>{{ device.risk }}</td>

                    <td>{{ device.last_seen }}</td>

                    <td>

                        <a
                        href="{{ url_for('delete_device', device_id=device.id) }}"
                        class="delete-btn"
                        >

                            Delete

                        </a>

                    </td>

                </tr>

                {% endfor %}

            </tbody>

        </table>

    </div>

</div>

{% endblock %}